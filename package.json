{
  "name": "tab-cleaner",
  "version": "1.0.2",
  "description": "Automatically closes inactive tabs to keep your browser clean and fast",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  },
  "author": "Jake Spurlock",
  "private": true
}
